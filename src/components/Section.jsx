import {Buttons} from "./Buttons"

export const Section = () => {
    return(
        <section>
            <div className="zero">
                0
            </div>

            <Buttons />
        </section>
    )
}